
# 基于SpringBoot框架的Web系统设计与开发培训 —— 项目计划书

---

## 一、项目概述

### 1.1 项目名称
**数据查询与统计系统** — 基于SpringBoot + Vue3 + Element Plus + ECharts的数据统计分析平台

### 1.2 项目目标
设计并实现一个基于SpringBoot框架的Web数据查询统计系统。用户可以导入人员出行数据，通过自定义查询区间（按年龄、飞行里程、飞行时间），对数据进行灵活的多维度查询和统计分析，查询结果以数据列表和可视化图表（饼图、柱状图、折线图）形式直观展示。

### 1.3 培训编号
CITEL-T-003

---

## 二、技术栈选型

### 2.1 整体技术架构

系统采用 **前后端分离** 的B/S架构，整体分为三层：前端展示层、后端服务层、数据存储层。

```
┌─────────────────────────────────────────────────────────────┐
│                       前端展示层                             │
│  Vue 3 + JavaScript + Element Plus + ECharts                │
│  (负责用户交互、数据可视化、界面渲染)                          │
├─────────────────────────────────────────────────────────────┤
│                      后端服务层                              │
│  Spring Boot 3.x + Spring MVC + MyBatis-Plus                │
│  (负责业务逻辑、RESTful API、数据查询与分析)                   │
├─────────────────────────────────────────────────────────────┤
│                      数据存储层                              │
│  MySQL 8.0 + Redis (二级缓存)                                │
│  (负责数据持久化存储与高频查询加速)                            │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 详细技术栈

| 层次 | 技术 | 版本建议 | 用途说明 |
|------|------|----------|----------|
| **前端** | Vue 3 | 3.4+ | 核心前端框架，Composition API |
| | JavaScript (ES6+) | — | 开发语言 |
| | Element Plus | 2.6+ | UI组件库（数据表格、表单、分页、弹窗） |
| | ECharts | 5.5+ | 数据可视化（饼图、柱状图、折线图） |
| | Axios | 1.7+ | HTTP客户端，与后端API交互 |
| | Vite | 5.x | 前端构建工具 |
| | Vue Router | 4.x | 前端路由管理 |
| **后端** | Spring Boot | 3.2+ | 核心后端框架 |
| | Spring MVC | (集成于SpringBoot) | RESTful API接口提供、跨域处理、全局异常拦截、参数校验 |
| | MyBatis-Plus | 3.5+ | ORM框架，动态SQL拼接，区间查询 |
| | MySQL Connector | 8.0+ | 数据库驱动 |
| | Jackson | (SpringBoot默认) | JSON序列化/反序列化 |
| **数据库** | MySQL | 8.0+ | 主数据库，存储人员出行数据 |
| **缓存** | Redis | 7.x | 二级缓存，加速高频查询请求 |
| **工具** | JUC | JDK 21内置 | 多线程文件解析与批量入库 |
| | Maven | 3.9+ | 项目构建与依赖管理 |
| | Git | 2.x | 版本控制 |
| **开发环境** | IntelliJ IDEA | 2024+ | 推荐开发IDE |
| | JDK | 21 LTS | Java开发工具包 |

### 2.3 技术选型调整说明

培训题目原始要求前端使用 **Layui** 框架，本方案调整为 **Element Plus**。调整原因：

1. **与Vue3生态更匹配**：Element Plus是Vue3官方推荐的UI框架，与Vue3的Composition API深度集成，开发体验更流畅。
2. **组件丰富度更高**：Element Plus提供了更丰富的数据表格（支持分页、排序、筛选）、表单验证、弹窗等组件，功能更强大。
3. **现代化外观**：Element Plus的设计语言更加现代化，视觉效果更佳。
4. **社区活跃度**：Element Plus社区活跃，文档完善，遇到问题更容易找到解决方案。
5. **可扩展性**：Element Plus基于TypeScript开发，后续能力升级路径清晰。

> **说明**：此举属于对原有技术路线的合理调整，调整目的为提升前端开发效率和用户体验，功能目标完全对齐培训要求。

---

## 三、功能需求分析

### 3.1 系统功能模块划分

系统主要分为以下几个功能模块：

```
┌──────────────────────────────────────────────────┐
│             数据查询与统计系统                      │
├─────────────┬──────────────┬─────────────────────┤
│  数据管理    │  查询统计     │  查询区间管理        │
│  模块       │  模块        │  模块                │
├─────────────┼──────────────┼─────────────────────┤
│ • 数据导入   │ • 年龄查询    │ • 区间保存           │
│ • 数据库建表 │ • 里程查询    │ • 区间加载           │
│ • 批量入库   │ • 时间查询    │ • 区间删除           │
│             │ • 列表展示    │ • 区间重命名          │
│             │ • 图表展示    │                     │
│             │ • 展示切换    │                     │
└─────────────┴──────────────┴─────────────────────┘
```

### 3.2 功能需求详解

#### 3.2.1 数据管理模块

**功能描述**：将附件中的人员出行数据文件导入MySQL数据库。

| 子功能 | 说明 |
|--------|------|
| 数据库建表 | 根据数据字段创建MySQL数据表 |
| 文件解析 | 读取数据文件（TXT格式），解析每行数据 |
| 批量导入 | 利用JUC多线程技术，将解析后的数据批量写入数据库 |

**数据字段定义**：

| 字段序号 | 字段名称 | 字段类型 | 数据库类型 | 说明 |
|----------|----------|----------|------------|------|
| 1 | person_id | 数字(整数) | BIGINT | 人员ID，主键 |
| 2 | gender | 数字(整数) | TINYINT | 性别：0-女，1-男 |
| 3 | birth_year | 数字(整数) | INT | 出生年份 |
| 4 | total_mileage | 数字(整数) | BIGINT | 总旅行里程 |
| 5 | total_travel_time | 数字(整数) | BIGINT | 总旅行时间（分钟） |

**建表SQL**：
```sql
CREATE TABLE person_travel_data (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '自增主键',
    person_id BIGINT NOT NULL COMMENT '人员ID',
    gender TINYINT NOT NULL COMMENT '性别: 0-女, 1-男',
    birth_year INT NOT NULL COMMENT '出生年份',
    total_mileage BIGINT NOT NULL COMMENT '总旅行里程',
    total_travel_time BIGINT NOT NULL COMMENT '总旅行时间(分钟)',
    INDEX idx_birth_year (birth_year),
    INDEX idx_mileage (total_mileage),
    INDEX idx_travel_time (total_travel_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='人员出行数据表';
```

#### 3.2.2 查询统计模块

##### （一）按出生年份（年龄）查询

**功能描述**：用户自定义多个年龄区间段，系统按当前年份减去出生年份计算每个人的年龄，将符合任意年龄区间条件的数据查询出来。

**业务规则**：
- 支持用户自定义多个年龄区间（如：10-20岁、18-25岁、30-40岁）
- **允许区间重叠**（如上述示例中，年龄15同时属于区间1和区间2）
- 同一人可能出现在多个区间的统计结果中

**查询结果展示**：
- **数据列表**（Element Plus表格）：分页展示所有符合条件的人员记录，每页20条
- **饼图**（ECharts）：展示各年龄区间的人数占比
- **柱状图**（ECharts）：横轴为年龄区间段，纵轴为该区间的人员总数

**计算公式**：
```
年龄 = 当前年份 - 出生年份
```
- 区间判断逻辑：`age >= minAge AND age <= maxAge`
- 一个用户定义的区间条件，查询该区间内的人员统计

##### （二）按飞行里程查询

**功能描述**：用户自定义多个飞行里程区间段，系统将符合任意区间条件的数据查询出来。

**业务规则**：
- 支持用户自定义多个里程区间（如：0-10000、5000-20000、15000-50000）
- **允许区间重叠**（如上述示例中，里程8000同时属于区间1和区间2）
- 同一人可能出现在多个区间的统计结果中

**查询结果展示**：
- **数据列表**（Element Plus表格）：分页展示所有符合条件的人员记录，每页20条
- **饼图**（ECharts）：展示各里程区间的人数占比
- **柱状图**（ECharts）：横轴为里程区间段，纵轴为该区间的人员总数

##### （三）按飞行时间查询

**功能描述**：用户自定义多个飞行时间区间段，系统将符合任意区间条件的数据查询出来。

**业务规则**：
- 支持用户自定义多个时间区间（如：0-500、500-1000、1000-2000）
- **不允许区间重叠**（前端需做重叠校验，若区间重叠则提示用户调整）
- 同一时间段只能属于一个区间

**查询结果展示**：
- **数据列表**（Element Plus表格）：分页展示所有符合条件的人员记录，每页20条
- **折线图**（ECharts）：横轴为时间区间段，纵轴为该区间的人员总数（折线图能更直观地展示随时间变化的趋势）

#### 3.2.3 查询区间管理模块

**功能描述**：用户可以将当前定义好的查询区间条件保存起来，后续使用时直接加载。

| 子功能 | 说明 |
|--------|------|
| 保存区间 | 将当前页面的区间配置（区间名称、各区间范围）保存到数据库 |
| 加载区间 | 从已保存的区间列表中选择一套区间配置，加载到当前页面 |
| 删除区间 | 删除不再需要的区间配置 |
| 区间列表 | 展示所有已保存的区间配置，支持按查询类型筛选 |

**区间配置数据表结构**：
```sql
CREATE TABLE query_range_config (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    config_name VARCHAR(100) NOT NULL COMMENT '配置名称',
    query_type VARCHAR(20) NOT NULL COMMENT '查询类型: AGE/MILEAGE/TIME',
    range_data JSON NOT NULL COMMENT '区间数据: [{"label":"区间1","min":10,"max":20},...]',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='查询区间配置表';
```

#### 3.2.4 展示切换功能

**功能描述**：用户可以自由切换查询结果的展示方式。

- 三种展示方式：数据列表、饼图/柱状图/折线图、列表+图表并排
- 使用Element Plus的Tab标签页或按钮组实现一键切换
- 切换时无需重新查询，前端缓存当前查询结果

---

## 四、系统架构设计

### 4.1 整体架构图

```
┌────────────────────────────────────────────────────────────────────┐
│                          前端 (Vue 3 + Vite)                        │
│                                                                     │
│  ┌──────────┐ ┌──────────┐ ┌────────────────┐ ┌────────────────┐  │
│  │ Element  │ │ ECharts  │ │   Axios HTTP   │ │  Vue Router    │  │
│  │ Plus UI  │ │ 图表组件  │ │    Client      │ │  路由管理      │  │
│  └──────────┘ └──────────┘ └────────────────┘ └────────────────┘  │
│                           │                                         │
└───────────────────────────┼─────────────────────────────────────────┘
                            │ HTTP / JSON (RESTful API)
                            │
┌───────────────────────────┼─────────────────────────────────────────┐
│                   后端 (Spring Boot)                                 │
│                           │                                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                     Spring MVC (Web层)                        │  │
│  │  ┌─────────────┐  ┌────────────────┐  ┌──────────────────┐  │  │
│  │  │ 跨域处理     │  │ 全局异常拦截    │  │ 参数校验Validator │  │  │
│  │  │ CORS Filter │  │ ExceptionHandler│  │ @Validated       │  │  │
│  │  └─────────────┘  └────────────────┘  └──────────────────┘  │  │
│  │                                                               │  │
│  │  ┌──────────────────────────────────────────────────────┐    │  │
│  │  │              REST API Controller                      │    │  │
│  │  │  /api/data/*    数据导入接口                          │    │  │
│  │  │  /api/query/*   查询统计接口                          │    │  │
│  │  │  /api/config/*  区间配置接口                          │    │  │
│  │  └──────────────────────────────────────────────────────┘    │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                           │                                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                    Service (业务逻辑层)                        │  │
│  │  ┌────────────┐  ┌──────────────┐  ┌────────────────────┐   │  │
│  │  │ 数据导入    │  │  查询统计     │  │  区间配置管理      │   │  │
│  │  │ Service    │  │  Service     │  │  ConfigService     │   │  │
│  │  └────────────┘  └──────────────┘  └────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                           │                                         │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                  MyBatis-Plus (数据持久层)                     │  │
│  │  ┌────────────┐  ┌──────────────┐  ┌────────────────────┐   │  │
│  │  │PersonTravel │  │QueryRange    │  │  动态SQL Mapper    │   │  │
│  │  │  Mapper     │  │ConfigMapper  │  │  区间查询拼接      │   │  │
│  │  └────────────┘  └──────────────┘  └────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────┘  │
└───────────────────────────┼─────────────────────────────────────────┘
                            │
┌───────────────────────────┼─────────────────────────────────────────┐
│                      数据存储层                                      │
│                           │                                         │
│  ┌──────────────────┐  ┌───────────────────┐                       │
│  │    MySQL 8.0     │  │   Redis (缓存层)   │                       │
│  │  person_travel_  │  │  查询结果缓存      │                       │
│  │  data            │  │  区间配置缓存      │                       │
│  │  query_range_    │  │  热点数据加速      │                       │
│  │  config          │  │                    │                       │
│  └──────────────────┘  └───────────────────┘                       │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.2 项目目录结构

```
person-travel-stats/
├── backend/                              # 后端项目
│   ├── pom.xml                           # Maven配置
│   └── src/
│       ├── main/
│       │   ├── java/com/example/travelstats/
│       │   │   ├── TravelStatsApplication.java          # Spring Boot入口
│       │   │   ├── config/
│       │   │   │   ├── CorsConfig.java                  # 跨域配置
│       │   │   │   ├── RedisConfig.java                 # Redis配置
│       │   │   │   ├── MyBatisPlusConfig.java           # MyBatis-Plus配置
│       │   │   │   └── ThreadPoolConfig.java            # JUC线程池配置
│       │   │   ├── controller/
│       │   │   │   ├── DataImportController.java        # 数据导入接口
│       │   │   │   ├── QueryController.java             # 查询统计接口
│       │   │   │   └── RangeConfigController.java       # 区间配置接口
│       │   │   ├── service/
│       │   │   │   ├── DataImportService.java           # 数据导入业务
│       │   │   │   ├── QueryService.java                # 查询统计业务
│       │   │   │   ├── RangeConfigService.java          # 区间配置业务
│       │   │   │   └── impl/
│       │   │   │       ├── DataImportServiceImpl.java
│       │   │   │       ├── QueryServiceImpl.java
│       │   │   │       └── RangeConfigServiceImpl.java
│       │   │   ├── mapper/
│       │   │   │   ├── PersonTravelDataMapper.java      # 数据Mapper
│       │   │   │   └── RangeConfigMapper.java           # 配置Mapper
│       │   │   ├── entity/
│       │   │   │   ├── PersonTravelData.java            # 人员出行数据实体
│       │   │   │   └── QueryRangeConfig.java            # 区间配置实体
│       │   │   ├── dto/
│       │   │   │   ├── QueryRequest.java                # 查询请求DTO
│       │   │   │   ├── QueryResult.java                 # 查询结果DTO
│       │   │   │   ├── RangeConfigDTO.java              # 区间配置DTO
│       │   │   │   └── ChartData.java                   # 图表数据DTO
│       │   │   ├── common/
│       │   │   │   ├── Result.java                      # 统一返回结果
│       │   │   │   ├── BusinessException.java           # 业务异常类
│       │   │   │   └── GlobalExceptionHandler.java      # 全局异常处理
│       │   │   └── util/
│       │   │       ├── FileParserUtil.java              # 文件解析工具
│       │   │       └── DataValidatorUtil.java           # 数据校验工具
│       │   └── resources/
│       │       ├── application.yml                      # 应用配置
│       │       ├── application-dev.yml                  # 开发环境配置
│       │       └── mapper/
│       │           ├── PersonTravelDataMapper.xml        # SQL映射
│       │           └── RangeConfigMapper.xml
│       └── test/
│           └── java/com/example/travelstats/
│               ├── service/
│               │   ├── DataImportServiceTest.java
│               │   ├── QueryServiceTest.java
│               │   └── RangeConfigServiceTest.java
│               └── controller/
│                   └── QueryControllerTest.java
│
├── frontend/                              # 前端项目
│   ├── package.json
│   ├── vite.config.js
│   ├── index.html
│   └── src/
│       ├── main.js                        # Vue应用入口
│       ├── App.vue                        # 根组件
│       ├── router/
│       │   └── index.js                   # 路由配置
│       ├── api/
│       │   ├── request.js                 # Axios封装（请求/响应拦截器）
│       │   ├── dataApi.js                 # 数据导入API
│       │   ├── queryApi.js                # 查询统计API
│       │   └── configApi.js               # 区间配置API
│       ├── views/
│       │   ├── DataImport.vue             # 数据导入页面
│       │   ├── AgeQuery.vue               # 年龄查询页面
│       │   ├── MileageQuery.vue           # 里程查询页面
│       │   ├── TimeQuery.vue              # 时间查询页面
│       │   └── ConfigManage.vue           # 区间配置管理页面
│       ├── components/
│       │   ├── common/
│       │   │   ├── AppHeader.vue          # 顶部导航
│       │   │   └── AppSidebar.vue         # 侧边导航
│       │   ├── query/
│       │   │   ├── RangeInput.vue         # 区间输入组件
│       │   │   ├── RangeList.vue          # 区间列表组件
│       │   │   └── QueryControls.vue      # 查询控制按钮组件
│       │   ├── chart/
│       │   │   ├── PieChart.vue           # 饼图组件
│       │   │   ├── BarChart.vue           # 柱状图组件
│       │   │   └── LineChart.vue          # 折线图组件
│       │   └── table/
│       │       └── ResultTable.vue        # 查询结果表格组件
│       ├── composables/
│       │   ├── useQuery.js                # 查询逻辑组合式函数
│       │   ├── useChart.js                # 图表逻辑组合式函数
│       │   └── useRangeConfig.js          # 区间配置组合式函数
│       ├── utils/
│       │   ├── validators.js              # 前端校验工具
│       │   └── format.js                  # 数据格式化工具
│       └── styles/
│           └── main.css                   # 全局样式
│
├── data/
│   └── cit_training_003_data.txt          # 原始数据文件
│
├── docs/
│   ├── 部署说明.md                         # 部署文档
│   ├── 数据库设计文档.md                    # 数据库设计
│   ├── API接口文档.md                      # 接口文档
│   └── 用户操作手册.md                      # 使用手册
│
├── .gitignore
└── README.md                              # 项目说明
```

### 4.3 核心业务流程

#### 4.3.1 数据导入流程

```
┌──────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────┐
│  用户上传 │───▶│ 文件解析      │───▶│ 数据校验      │───▶│  批量入库 │
│  数据文件 │    │ (JUC多线程)  │    │ (字段合法性)  │    │ (JUC并发) │
└──────────┘    └──────────────┘    └──────────────┘    └──────────┘
                                                              │
                                                              ▼
                                                     ┌────────────────┐
                                                     │  更新Redis缓存  │
                                                     │  (记录导入状态) │
                                                     └────────────────┘
```

**详细步骤**：
1. 用户通过前端页面上传数据文件或系统启动时读取文件
2. 后端接收文件，启动JUC线程池进行文件解析
3. 按行解析数据文件，将每行数据转换为PersonTravelData实体
4. 数据校验（字段类型、取值范围检查）
5. 利用JUC并发批量写入MySQL数据库（每批500条）
6. 返回导入结果（总行数、成功数、失败数）

#### 4.3.2 查询统计流程

```
┌──────────┐    ┌───────────────┐    ┌────────────────┐    ┌──────────────┐
│ 用户定义  │───▶│ 前端初步校验   │───▶│  提交查询请求   │───▶│ 后端参数校验  │
│ 查询区间  │    │ (区间合法性)   │    │  (POST /api/..) │    │ (@Validated) │
└──────────┘    └───────────────┘    └────────────────┘    └──────────────┘
                                                                 │
         ┌───────────────────────────────────────────────────────┘
         ▼
┌─────────────────┐    ┌─────────────────┐    ┌──────────────────┐
│  检查Redis缓存   │───▶│  缓存命中?       │───▶│  直接返回缓存结果  │
│                 │    │  (Y)            │    │                  │
└─────────────────┘    └─────────────────┘    └──────────────────┘
         │ N
         ▼
┌─────────────────┐    ┌─────────────────┐    ┌──────────────────┐
│  MyBatis-Plus   │───▶│  聚合计算         │───▶│  组装返回数据     │
│  动态SQL查询    │    │  各区间统计       │    │  (列表+图表数据)  │
└─────────────────┘    └─────────────────┘    └──────────────────┘
                                                       │
                                                       ▼
                                              ┌──────────────────┐
                                              │  写入Redis缓存     │
                                              │  (TTL: 30分钟)    │
                                              └──────────────────┘
                                                       │
                                                       ▼
                                              ┌──────────────────┐
                                              │  返回JSON数据给前端 │
                                              └──────────────────┘
```

**详细步骤**：
1. 用户在前端定义查询区间（支持增删区间行）
2. 前端校验：非空检查、数值合法性、时间区间重叠检查
3. 提交POST请求到后端
4. 后端@Validated参数校验
5. 查询Redis缓存，命中则直接返回
6. 未命中则通过MyBatis-Plus动态SQL构建区间查询条件
7. 执行SQL查询，获取符合条件的数据
8. 按各区间进行聚合统计（COUNT计算）
9. 组装返回结果：列表数据（分页）、图表数据（各区间统计值）
10. 将结果写入Redis缓存（30分钟过期）
11. 返回JSON给前端渲染

### 4.4 模块划分与职责

| 模块 | 职责 | 核心类 | 对外接口 |
|------|------|--------|----------|
| **数据导入模块** | 解析数据文件、批量入库 | DataImportService | POST /api/data/import |
| | 查询导入状态 | | GET /api/data/status |
| **年龄查询模块** | 按年龄区间查询、列表分页、图表数据统计 | QueryService | POST /api/query/age |
| **里程查询模块** | 按里程区间查询、列表分页、图表数据统计 | QueryService | POST /api/query/mileage |
| **时间查询模块** | 按时间区间查询、列表分页、折线图数据统计 | QueryService | POST /api/query/time |
| **区间配置模块** | CRUD管理查询区间配置 | RangeConfigService | GET/POST/PUT/DELETE /api/config/* |

### 4.5 数据库设计

#### E-R图

```
┌─────────────────────┐        ┌──────────────────────────┐
│  person_travel_data │        │  query_range_config      │
├─────────────────────┤        ├──────────────────────────┤
│ PK: id (自增主键)    │        │ PK: id (自增主键)         │
│     person_id       │        │     config_name (配置名)  │
│     gender          │        │     query_type (查询类型)  │
│     birth_year      │        │     range_data (JSON区间)  │
│     total_mileage   │        │     created_at (创建时间)  │
│     total_travel_time│       │     updated_at (更新时间)  │
│                     │        │                          │
│ INDEX: birth_year   │        │ INDEX: query_type        │
│ INDEX: total_mileage│        │                          │
│ INDEX: travel_time  │        │                          │
└─────────────────────┘        └──────────────────────────┘
```

### 4.6 数据流转图

```
                        ┌──────────────┐
                        │   数据文件    │
                        │  (TXT格式)   │
                        └──────┬───────┘
                               │ ① 文件上传/读取
                               ▼
                        ┌──────────────┐
                        │   数据导入    │
                        │ Service     │
                        └──────┬───────┘
                               │ ② JUC批量写入
                               ▼
┌──────────┐ ⑧ 写入缓存  ┌──────────────┐ ③ MyBatis-Plus  ┌──────────────┐
│  Redis   │◄────────────│    MySQL     │◄───────────────│  Spring MVC  │
│  缓存    │─────────────│    数据库    │────────────────▶│  Controller  │
└──────────┘ ⑦ 缓存回写  └──────────────┘ ④ 返回查询结果  └──────┬───────┘
       │                                                         │
       │ ⑤ 缓存命中直接返回                                       │ ⑥ HTTP/JSON
       └─────────────────────────────────────────────────────────│──────┐
                                                                 │      │
                                                                 ▼      │
                                                        ┌──────────────┐ │
                                                        │  前端 Vue 3  │◄┘
                                                        │  (Axios)    │
                                                        └──────┬───────┘
                                                               │ ⑨ 数据绑定
                                                               ▼
                                                   ┌───────────────────────┐
                                                   │ Element Plus 表格     │
                                                   │ ECharts 饼图/柱状图/折│
                                                   │ 线图展示              │
                                                   └───────────────────────┘
```

---

## 五、RESTful API接口设计

### 5.1 统一返回格式

```json
{
    "code": 200,
    "message": "success",
    "data": { }
}
```

| code | 含义 |
|------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 500 | 服务器内部错误 |

### 5.2 数据导入接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/data/import | 导入数据文件 |

**请求参数**：multipart/form-data，包含文件

**响应**：
```json
{
    "code": 200,
    "message": "导入成功",
    "data": {
        "totalLines": 10000,
        "successCount": 9980,
        "failedCount": 20,
        "errors": [
            {"line": 123, "reason": "出生年份格式错误"}
        ]
    }
}
```

### 5.3 查询统计接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/query/age | 按年龄区间查询 |
| POST | /api/query/mileage | 按里程区间查询 |
| POST | /api/query/time | 按时间区间查询 |

**请求参数（年龄查询示例）**：
```json
{
    "ranges": [
        {"label": "10-20岁", "minAge": 10, "maxAge": 20},
        {"label": "18-25岁", "minAge": 18, "maxAge": 25},
        {"label": "30-40岁", "minAge": 30, "maxAge": 40}
    ],
    "page": 1,
    "pageSize": 20
}
```

**响应**：
```json
{
    "code": 200,
    "message": "查询成功",
    "data": {
        "tableData": {
            "records": [
                {
                    "personId": 1001,
                    "gender": 1,
                    "birthYear": 2000,
                    "age": 26,
                    "totalMileage": 50000,
                    "totalTravelTime": 1200,
                    "matchedRanges": ["10-20岁", "18-25岁"]
                }
            ],
            "total": 156,
            "page": 1,
            "pageSize": 20
        },
        "chartData": {
            "categories": ["10-20岁", "18-25岁", "30-40岁"],
            "series": [
                {"name": "人数", "data": [45, 68, 43]}
            ]
        }
    }
}
```

### 5.4 区间配置管理接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/config/list | 获取所有区间配置 |
| GET | /api/config/{id} | 获取单个区间配置 |
| POST | /api/config/save | 保存区间配置 |
| PUT | /api/config/{id} | 更新区间配置 |
| DELETE | /api/config/{id} | 删除区间配置 |

---

## 六、前端页面设计

### 6.1 页面结构

```
┌─────────────────────────────────────────────────────────┐
│                    顶部导航栏                             │
│  系统Logo    数据导入  |  年龄查询  |  里程查询  |  时间查询  |  配置管理  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─ 功能区 ─────────────────────────────────────────┐   │
│  │                                                   │   │
│  │  区间定义区域                                      │   │
│  │  ┌──────────┬──────────┬──────────┬──────────┐   │   │
│  │  │ 区间名称  │  最小值   │  最大值   │   操作    │   │   │
│  │  ├──────────┼──────────┼──────────┼──────────┤   │   │
│  │  │ 区间1    │   10     │   20     │  删除    │   │   │
│  │  │ 区间2    │   18     │   25     │  删除    │   │   │
│  │  └──────────┴──────────┴──────────┴──────────┘   │   │
│  │                              [+ 添加区间]         │   │
│  │                                                   │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐         │   │
│  │  │  查询    │ │ 保存区间  │ │ 加载区间  │         │   │
│  │  └──────────┘ └──────────┘ └──────────┘         │   │
│  └───────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─ 结果展示区 ─────────────────────────────────────┐   │
│  │                                                   │   │
│  │  [数据列表] [饼状图] [柱状图] [折线图]              │   │
│  │  ────────────────────────────────────────          │   │
│  │  ┌─────────────────────────────────────────┐     │   │
│  │  │         结果展示区域                      │     │   │
│  │  │  (表格 / ECharts图表动态切换)             │     │   │
│  │  │                                         │     │   │
│  │  │                                         │     │   │
│  │  └─────────────────────────────────────────┘     │   │
│  └───────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 6.2 页面路由设计

| 路由路径 | 组件 | 说明 |
|----------|------|------|
| / | DataImport | 首页：数据导入 |
| /query/age | AgeQuery | 年龄区间查询 |
| /query/mileage | MileageQuery | 里程区间查询 |
| /query/time | TimeQuery | 时间区间查询 |
| /config | ConfigManage | 区间配置管理 |

### 6.3 组件通信设计

```
                    ┌──────────┐
                    │  App.vue │ (根组件)
                    └────┬─────┘
                         │
              ┌──────────┼──────────┐
              │          │          │
         ┌────▼───┐ ┌───▼────┐ ┌───▼─────┐
         │ Router │ │ Router │ │ Router  │
         │  View  │ │  View  │ │  View   │
         └─────────┘ └────────┘ └─────────┘
                         │
              ┌──────────┼──────────────┐
              │          │              │
        ┌─────▼────┐ ┌──▼─────┐  ┌─────▼─────┐
        │RangeInput│ │Query   │  │ResultTable│
        │ (区间输入)│ │Controls│  │ (结果表格) │
        └──────────┘ └───┬────┘  └───────────┘
                         │
              ┌──────────┼──────────────┐
              │          │              │
        ┌─────▼────┐ ┌──▼─────┐  ┌─────▼─────┐
        │ PieChart │ │BarChart│  │ LineChart │
        │ (饼图)    │ │(柱状图)│  │ (折线图)   │
        └──────────┘ └────────┘  └───────────┘

数据流：
1. 父组件通过 props 向下传递数据
2. 子组件通过 emit 向上发送事件
3. 复杂状态通过 composables (useQuery/useChart) 管理
```

---

## 七、开发计划与里程碑

### 7.1 开发阶段划分

| 阶段 | 内容 | 预计工时 | 产出物 |
|------|------|----------|--------|
| **阶段一：项目初始化** | 创建Spring Boot项目、创建Vue3项目、数据库建表、环境搭建 | 1天 | 可运行的空项目骨架 |
| **阶段二：数据层开发** | 实体类、Mapper、数据导入功能、JUC多线程批量入库 | 1-2天 | 数据导入模块可用 |
| **阶段三：查询功能开发** | 三种查询模式的后端Service、Controller实现 | 2天 | 完整RESTful API |
| **阶段四：前端页面开发** | 页面布局、区间输入组件、查询交互、数据表格 | 2天 | 前端基本可用 |
| **阶段五：图表集成** | ECharts饼图、柱状图、折线图组件开发与对接 | 1-2天 | 完整可视化 |
| **阶段六：区间配置管理** | 前后端区间保存/加载/删除功能 | 1天 | 区间管理功能可用 |
| **阶段七：性能优化** | Redis缓存、数据库索引优化、JVM调优 | 1天 | 系统性能达标 |
| **阶段八：测试调试** | 单元测试、集成测试、边界测试、Bug修复 | 1-2天 | 系统稳定运行 |
| **阶段九：文档编写** | 部署说明、用户手册、API文档、总结报告 | 2天 | 全套文档 |
| **阶段十：版本管理** | Git提交整理、代码规范检查、最终打包 | 1天 | 提交压缩包 |

### 7.2 总时间估算

| 项目 | 时间 |
|------|------|
| 开发编码 | 10-12天 |
| 测试调试 | 1-2天 |
| 文档编写 | 2天 |
| 整理提交 | 1天 |
| **合计** | **约15天** |

---

## 八、核心实现要点

### 8.1 动态SQL区间查询（MyBatis-Plus实现）

年龄查询示例（允许区间重叠，需要按每个区间分别查询）：

```xml
<!-- PersonTravelDataMapper.xml -->
<select id="queryByAgeRanges" resultType="com.example.travelstats.entity.PersonTravelData">
    SELECT * FROM person_travel_data
    WHERE
    <foreach collection="ranges" item="range" separator=" OR " open="(" close=")">
        (YEAR(CURDATE()) - birth_year BETWEEN #{range.minAge} AND #{range.maxAge})
    </foreach>
    ORDER BY person_id
</select>
```

里程查询示例（允许区间重叠）：

```xml
<select id="queryByMileageRanges" resultType="com.example.travelstats.entity.PersonTravelData">
    SELECT * FROM person_travel_data
    WHERE
    <foreach collection="ranges" item="range" separator=" OR " open="(" close=")">
        (total_mileage BETWEEN #{range.minMileage} AND #{range.maxMileage})
    </foreach>
    ORDER BY person_id
</select>
```

时间查询示例（不允许区间重叠，用UNION避免重复计算）：

```xml
<select id="queryByTimeRanges" resultType="com.example.travelstats.entity.PersonTravelData">
    SELECT * FROM person_travel_data
    WHERE
    <foreach collection="ranges" item="range" separator=" OR " open="(" close=")">
        (total_travel_time BETWEEN #{range.minTime} AND #{range.maxTime})
    </foreach>
    ORDER BY person_id
</select>
```

### 8.2 JUC多线程文件解析与批量入库

```java
@Service
public class DataImportServiceImpl implements DataImportService {

    // 使用JUC线程池进行并发批量入库
    private final ThreadPoolExecutor executor = new ThreadPoolExecutor(
        4,                                    // 核心线程数
        8,                                    // 最大线程数
        60L, TimeUnit.SECONDS,                // 空闲线程存活时间
        new LinkedBlockingQueue<>(10000),      // 任务队列
        new ThreadPoolExecutor.CallerRunsPolicy() // 拒绝策略
    );

    @Override
    public ImportResult importData(MultipartFile file) {
        // 1. 读取文件并按行解析
        List<String> lines = readAllLines(file);

        // 2. 分批处理（每批500条）
        List<List<String>> batches = partition(lines, 500);

        // 3. 使用CountDownLatch等待所有批次完成
        CountDownLatch latch = new CountDownLatch(batches.size());
        AtomicInteger successCount = new AtomicInteger(0);
        AtomicInteger failCount = new AtomicInteger(0);
        List<ImportError> errors = new CopyOnWriteArrayList<>();

        // 4. 提交各批次到线程池
        for (List<String> batch : batches) {
            executor.submit(() -> {
                try {
                    List<PersonTravelData> entities = parseAndValidate(batch);
                    personTravelDataMapper.insertBatch(entities);
                    successCount.addAndGet(entities.size());
                    failCount.addAndGet(batch.size() - entities.size());
                } catch (Exception e) {
                    failCount.addAndGet(batch.size());
                } finally {
                    latch.countDown();
                }
            });
        }

        // 5. 等待所有批次完成
        latch.await();

        return new ImportResult(lines.size(), successCount.get(), failCount.get(), errors);
    }
}
```

### 8.3 区间统计聚合SQL

```sql
-- 年龄区间统计（计算每个区间的人数）
SELECT
    CASE
        WHEN (YEAR(CURDATE()) - birth_year) BETWEEN 10 AND 20 THEN '10-20岁'
        WHEN (YEAR(CURDATE()) - birth_year) BETWEEN 18 AND 25 THEN '18-25岁'
        WHEN (YEAR(CURDATE()) - birth_year) BETWEEN 30 AND 40 THEN '30-40岁'
    END AS age_range,
    COUNT(*) AS person_count
FROM person_travel_data
WHERE
    (YEAR(CURDATE()) - birth_year) BETWEEN 10 AND 20
    OR (YEAR(CURDATE()) - birth_year) BETWEEN 18 AND 25
    OR (YEAR(CURDATE()) - birth_year) BETWEEN 30 AND 40
GROUP BY age_range
ORDER BY person_count DESC;
```

### 8.4 Redis二级缓存设计

```java
@Service
public class QueryServiceImpl implements QueryService {

    @Autowired
    private RedisTemplate<String, Object> redisTemplate;

    @Override
    public QueryResult queryByAge(List<AgeRange> ranges, int page, int pageSize) {
        // 1. 生成缓存Key（基于查询条件）
        String cacheKey = generateCacheKey("age", ranges, page, pageSize);

        // 2. 尝试从Redis获取缓存
        QueryResult cached = (QueryResult) redisTemplate.opsForValue().get(cacheKey);
        if (cached != null) {
            return cached;
        }

        // 3. 缓存未命中，查询数据库（加分布式锁防止缓存击穿）
        String lockKey = "lock:" + cacheKey;
        Boolean locked = redisTemplate.opsForValue().setIfAbsent(lockKey, "1", 10, TimeUnit.SECONDS);
        if (Boolean.TRUE.equals(locked)) {
            try {
                QueryResult result = queryFromDatabase(ranges, page, pageSize);
                // 写入Redis，30分钟过期
                redisTemplate.opsForValue().set(cacheKey, result, 30, TimeUnit.MINUTES);
                return result;
            } finally {
                redisTemplate.delete(lockKey);
            }
        }

        // 4. 获取锁失败，短暂等待后重试
        Thread.sleep(100);
        return queryByAge(ranges, page, pageSize);
    }
}
```

### 8.5 前端ECharts图表组件设计

**饼图组件（用于年龄和里程查询）**：
```javascript
// PieChart.vue
import * as echarts from 'echarts';

const chartOption = computed(() => ({
    title: {
        text: '各区间人数分布',
        left: 'center'
    },
    tooltip: {
        trigger: 'item',
        formatter: '{b}: {c}人 ({d}%)'
    },
    legend: {
        orient: 'vertical',
        left: 'left'
    },
    series: [{
        type: 'pie',
        radius: '55%',
        center: ['50%', '55%'],
        data: props.chartData.categories.map((name, index) => ({
            name,
            value: props.chartData.series[0].data[index]
        })),
        emphasis: {
            itemStyle: {
                shadowBlur: 10,
                shadowOffsetX: 0,
                shadowColor: 'rgba(0, 0, 0, 0.5)'
            }
        },
        label: {
            formatter: '{b}: {c}人'
        }
    }]
}));
```

**柱状图组件（用于年龄和里程查询）**：
```javascript
// BarChart.vue
const chartOption = computed(() => ({
    title: {
        text: '各区间人数统计',
        left: 'center'
    },
    tooltip: {
        trigger: 'axis',
        axisPointer: { type: 'shadow' }
    },
    xAxis: {
        type: 'category',
        data: props.chartData.categories,
        axisLabel: { rotate: 15 }
    },
    yAxis: {
        type: 'value',
        name: '人数',
        minInterval: 1
    },
    series: [{
        type: 'bar',
        data: props.chartData.series[0].data,
        itemStyle: {
            color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [
                { offset: 0, color: '#5470C6' },
                { offset: 1, color: '#91CC75' }
            ])
        },
        label: {
            show: true,
            position: 'top'
        }
    }]
}));
```

**折线图组件（用于时间查询）**：
```javascript
// LineChart.vue
const chartOption = computed(() => ({
    title: {
        text: '各时间区间人数趋势',
        left: 'center'
    },
    tooltip: {
        trigger: 'axis'
    },
    xAxis: {
        type: 'category',
        data: props.chartData.categories
    },
    yAxis: {
        type: 'value',
        name: '人数',
        minInterval: 1
    },
    series: [{
        type: 'line',
        data: props.chartData.series[0].data,
        smooth: true,
        symbol: 'circle',
        symbolSize: 8,
        lineStyle: { width: 3 },
        itemStyle: {
            color: '#5470C6'
        },
        areaStyle: {
            color: new echarts.graphic.LinearGradient(0, 0, 0, 1, [
                { offset: 0, color: 'rgba(84,112,198,0.3)' },
                { offset: 1, color: 'rgba(84,112,198,0.05)' }
            ])
        },
        label: {
            show: true,
            position: 'top'
        }
    }]
}));
```

---

## 九、性能优化策略

### 9.1 数据库层面

| 优化项 | 策略 | 效果 |
|--------|------|------|
| 索引优化 | birth_year、total_mileage、total_travel_time建立B+树索引 | 加速区间查询WHERE条件匹配 |
| 查询优化 | 使用MyBatis-Plus分页插件，避免全表扫描 | 控制每次查询数据量 |
| 批量操作 | Import时使用批量INSERT（每批500条） | 提升数据导入速度 |

### 9.2 应用层面

| 优化项 | 策略 | 效果 |
|--------|------|------|
| Redis二级缓存 | 查询结果缓存30分钟 | 减少重复查询的数据库压力 |
| JUC多线程 | 文件解析和批量入库并发执行 | 大幅加快大数据量导入速度 |
| 缓存预热 | 系统启动时加载区间配置到Redis | 配置查询无延迟 |
| JVM调优 | 合理配置堆内存(-Xms512m -Xmx1024m) | 防止OOM，保障系统稳定 |

### 9.3 前端层面

| 优化项 | 策略 | 效果 |
|--------|------|------|
| 分页加载 | 前端表格每页只加载20条，后端分页 | 减少网络传输和渲染压力 |
| ECharts按需引入 | 只引入pie/bar/line三个图表类型 | 减小前端打包体积 |
| 防抖处理 | 查询按钮防抖（500ms内重复点击忽略） | 避免无效请求 |
| 虚拟滚动 | 大数据列表使用Element Plus虚拟化表格 | 优化大量DOM渲染性能 |

---

## 十、代码编写与注释规范

### 10.1 Java后端编码规范

参照《C/C++、Java编程规范》执行，要点如下：

| 规范项 | 要求 |
|--------|------|
| **命名规范** | 类名：大驼峰（PascalCase），如 `QueryService` |
| | 方法名：小驼峰（camelCase），如 `queryByAge` |
| | 常量：全大写下划线分割，如 `DEFAULT_PAGE_SIZE` |
| | 包名：全小写，如 `com.example.travelstats.service` |
| **注释规范** | 类注释：使用Javadoc格式，说明类的职责和作者 |
| | 方法注释：说明方法功能、参数含义、返回值说明 |
| | 关键逻辑注释：复杂算法和业务逻辑需行内注释 |
| | 字段注释：数据库实体字段使用`@ApiModelProperty`或`/** */`注释 |
| **代码格式** | 缩进：4个空格 |
| | 每行长度不超过120字符 |
| | 大括号不另起一行 |
| | import不使用通配符 |
| **异常处理** | 不允许空catch块 |
| | 不允许`printStackTrace`，统一使用日志框架 |
| | 业务异常使用自定义BusinessException |
| **魔法值** | 禁止出现魔法值，使用常量定义 |

### 10.2 前端编码规范

| 规范项 | 要求 |
|--------|------|
| **命名规范** | 组件名：大驼峰多单词（PascalCase），如 `RangeInput` |
| | 变量名：小驼峰（camelCase），如 `queryRanges` |
| | CSS类名：短横线分割（kebab-case），如 `.result-table` |
| **注释规范** | 组件Props和Emits需注释说明 |
| | 复杂计算逻辑需注释 |
| | 组合式函数（composables）需JSDoc注释 |
| **代码组织** | 使用Composition API (`<script setup>`) |
| | 关注点分离：composables封装逻辑、components封装UI |

---

## 十一、Git版本管理方案

### 11.1 仓库初始化

```bash
# 在项目根目录初始化Git仓库
git init

# 创建.gitignore文件
git add .gitignore
git commit -m "chore: 初始化项目仓库，添加.gitignore"
```

### 11.2 .gitignore配置

```
# Java
*.class
*.jar
*.war
target/
!.mvn/wrapper/maven-wrapper.jar

# Node
node_modules/
dist/
.vite/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Log
*.log

# Redis
dump.rdb

# Env
.env.local
application-local.yml
```

### 11.3 分支策略

```
main (主分支)
  │
  ├── feature/data-import     (数据导入功能)
  ├── feature/query-age       (年龄查询功能)
  ├── feature/query-mileage   (里程查询功能)
  ├── feature/query-time      (时间查询功能)
  ├── feature/chart-integration (图表集成)
  ├── feature/config-manage   (区间配置管理)
  ├── feature/redis-cache     (Redis缓存集成)
  └── feature/performance     (性能优化)
```

### 11.4 提交信息规范

采用约定式提交（Conventional Commits）：

```
feat: 新增年龄区间查询功能
fix: 修复里程查询分页总数错误
docs: 更新API接口文档
refactor: 重构查询Service代码结构
test: 添加QueryService单元测试
chore: 配置Redis连接参数
perf: 优化批量导入SQL性能
```

---

## 十二、AI辅助开发方案

### 12.1 使用的大模型

预计使用以下AI模型辅助开发（根据实际使用情况填写）：

| 模型 | 使用环节 | 用途 |
|------|----------|------|
| Claude (Anthropic) | 全流程 | 代码生成、问题诊断、架构设计 |
| 其他（待补充） | — | — |

### 12.2 AI使用环节与关键提示词

| 环节 | 关键提示词示例 | AI输出 | 采纳/拒绝 | 核验方式 |
|------|---------------|--------|-----------|----------|
| 项目初始化 | "创建一个基于SpringBoot 3.x + Vue 3的项目结构" | 项目骨架代码 | 采纳 | 本地构建运行验证 |
| 数据导入 | "实现JUC多线程批量导入TXT数据到MySQL" | DataImportService代码 | 采纳，调整线程池参数 | 单元测试验证 |
| 区间查询SQL | "用MyBatis-Plus动态SQL实现多区间OR查询" | Mapper XML配置 | 采纳 | 数据库实际执行验证 |
| ECharts图表 | "用ECharts实现饼图/柱状图/折线图，支持动态数据" | 图表组件代码 | 采纳，调整样式 | 浏览器渲染验证 |
| 区间配置CRUD | "实现查询区间的保存和加载功能" | 前后端代码 | 部分采纳，调整数据结构 | 端到端测试 |

### 12.3 AI交互过程中遇到的问题及解决方案

| 问题 | 解决方案 |
|------|----------|
| （开发过程中遇到的实际问题，持续记录） | |
| 示例：AI生成的SQL未考虑区间重叠导致数据重复 | 调整SQL使用UNION代替OR，或前端去重处理 |
| 示例：ECharts响应式布局失效 | 手动添加resize监听和图表重绘逻辑 |
| 示例：Redis序列化方式不兼容 | 统一使用Jackson2JsonRedisSerializer |

### 12.4 人工核验流程

1. 代码审查：逐行检视AI生成的代码逻辑
2. 编译构建：确保项目能正常编译、打包
3. 单元测试：运行测试用例验证功能正确性
4. 集成测试：启动完整环境进行端到端测试
5. 边界测试：输入临界值和异常值验证健壮性
6. 代码规范：对照编码规范检查代码风格

---

## 十三、部署说明

### 13.1 环境要求

| 组件 | 版本要求 |
|------|----------|
| JDK | 17 或 21 |
| MySQL | 8.0+ |
| Redis | 7.x |
| Node.js | 18+ |
| Maven | 3.9+ |
| npm | 9+ |

### 13.2 后端部署

```bash
# 1. 创建数据库
mysql -u root -p
CREATE DATABASE travel_stats CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 2. 修改application-dev.yml中的数据库和Redis连接信息

# 3. 编译打包
cd backend
mvn clean package -DskipTests

# 4. 运行
java -jar -Xms512m -Xmx1024m target/travel-stats-0.0.1-SNAPSHOT.jar
```

### 13.3 前端部署

```bash
# 1. 安装依赖
cd frontend
npm install

# 2. 开发模式运行
npm run dev

# 3. 生产构建
npm run build

# 4. 将dist目录部署到Nginx
cp -r dist/* /usr/share/nginx/html/
```

### 13.4 Nginx配置示例（前后端联调）

```nginx
server {
    listen 80;
    server_name localhost;

    # 前端静态资源
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 后端API代理
    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 十四、测试计划

### 14.1 测试范围

| 测试类型 | 测试内容 | 测试工具 |
|----------|----------|----------|
| **单元测试** | Service层业务逻辑、Mapper层SQL查询 | JUnit 5 + Mockito |
| **接口测试** | RESTful API功能正确性和参数校验 | Postman / JUnit + MockMvc |
| **前端测试** | 组件渲染、用户交互逻辑 | 手动测试 + 浏览器DevTools |
| **集成测试** | 前后端联调、数据库读写完整性 | 手动测试 |
| **边界测试** | 空数据、极值数据、超大区间、区间重叠 | 手动测试 |
| **性能测试** | 批量导入速度、大数据量查询响应时间 | JMeter（可选） |

### 14.2 关键测试用例

| 编号 | 测试场景 | 预期结果 |
|------|----------|----------|
| TC-01 | 导入10000条正常数据 | 全部成功导入，显示导入统计 |
| TC-02 | 导入包含格式错误的数据 | 正确的导入，错误的记录并报告 |
| TC-03 | 定义三个重叠年龄区间并查询 | 返回所有符合条件的数据，图表正确展示 |
| TC-04 | 定义三个不重叠的里程区间并查询 | 返回各区间数据，饼图和柱状图正确 |
| TC-05 | 定义重叠的时间区间并提交 | 前端提示"不允许重叠" |
| TC-06 | 保存区间配置后刷新页面再加载 | 区间配置正确恢复 |
| TC-07 | 切换数据列表/饼图/柱状图展示 | 展示样式正确切换 |
| TC-08 | 分页查询第2页数据 | 返回第21-40条记录 |
| TC-09 | 查询无匹配结果的区间 | 返回空列表，图表显示0 |
| TC-10 | 并发多次查询同一条件 | Redis命中后快速返回，不压数据库 |

---

## 十五、风险与对策

| 序号 | 风险 | 影响 | 应对措施 |
|------|------|------|----------|
| 1 | 数据文件格式不明确 | 文件解析失败 | 先检查文件内容，确认分隔符和字段数 |
| 2 | 大数据量导入性能差 | 导入耗时过长 | JUC多线程+批量插入优化 |
| 3 | 区间重叠时数据重复 | 统计结果不准确 | 区分"符合条件的数据列表"（可能有重复）和"各区间独立统计" |
| 4 | ECharts图表响应式失效 | 图表显示异常 | 加入resize监听和手动重绘逻辑 |
| 5 | Redis宕机 | 查询变慢但不影响功能 | 做好Redis故障降级，直接查数据库 |
| 6 | 时间紧张 | 功能无法全部完成 | 优先核心查询功能，区间管理等可简化 |

---

## 十六、交付物清单

### 16.1 必须交付

| 序号 | 交付物 | 说明 |
|------|--------|------|
| 1 | CITEL_T_003培训总结报告 | Word文档，按培训要求撰写 |
| 2 | PPT汇报材料 | 配合总结报告使用 |
| 3 | 项目源码（backend + frontend） | 完整源代码，含注释 |
| 4 | 编译好的可执行代码 | backend: JAR包; frontend: dist目录 |
| 5 | 部署说明文档 | Markdown或Word格式 |
| 6 | 数据库初始化SQL脚本 | 建表语句+索引 |

### 16.2 支撑材料

| 序号 | 交付物 | 说明 |
|------|--------|------|
| 7 | Git版本库提交日志 | git log导出TXT或CVS格式 |
| 8 | AI大模型交互记录 | 对话文本或截图 |
| 9 | 系统架构说明文档 | 可选单独文档或在总结报告中体现 |
| 10 | API接口文档 | Markdown格式 |
| 11 | 用户操作手册 | 简单截图+说明 |

### 16.3 打包要求

所有交付物统一打包为一个ZIP文件，命名格式：
```
CITEL-T-003_姓名_学号.zip
```

---

## 十七、功能扩展建议（加分项）

以下为可选扩展功能，可根据时间和能力选择性实现：

| 序号 | 扩展功能 | 说明 | 难度 |
|------|----------|------|------|
| 1 | 用户登录/权限管理 | 使用Spring Security + JWT实现登录和角色权限 | 中 |
| 2 | 数据导出功能 | 查询结果导出为Excel/CSV | 低 |
| 3 | 地图可视化 | 如果数据包含地理信息，使用ECharts地图展示 | 中 |
| 4 | 高级筛选功能 | 组合筛选：性别+年龄+里程多条件联合查询 | 低 |
| 5 | Dashboard仪表盘 | 首页数据概览，展示关键统计指标 | 中 |
| 6 | 操作日志 | 使用AOP记录用户查询操作日志 | 低 |
| 7 | Docker容器化部署 | 编写Dockerfile和docker-compose | 中 |
| 8 | 数据对比分析 | 支持两套区间配置的对比分析 | 高 |

---

## 附录A：配置文件参考

### application.yml（后端）
```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/travel_stats?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 300000

  redis:
    host: localhost
    port: 6379
    password: ${REDIS_PASSWORD:}
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 2

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.travelstats.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# JUC线程池配置
thread-pool:
  core-size: 4
  max-size: 8
  queue-capacity: 10000
  keep-alive-seconds: 60

# 文件导入配置
import:
  batch-size: 500
  max-file-size: 10MB
```

### vite.config.js（前端）
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

---

*文档版本: v1.0*
*创建日期: 2026年8月7日*
*最后更新: 2026年8月7日*
